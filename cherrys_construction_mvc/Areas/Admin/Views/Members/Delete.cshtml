@model MemberResponce

@{
    ViewData["Title"] = Breadcrumb.MembersTitle;
}

<!-- ======= Breadcrumbs ======= -->
<div class="breadcrumbs d-flex align-items-center" style="background-image: url('/assets/img/breadcrumbs-bg.jpg');">
    <div class="container position-relative d-flex flex-column align-items-center" data-aos="fade">

        <h2>@Breadcrumb.MembersTitle</h2>
        <ol>
            <li><a asp-area="@Breadcrumb.HomeArea" asp-controller="@Breadcrumb.HomeController" asp-action="Index">@Breadcrumb.HomeTitle</a></li>
            <li><a asp-area="@Breadcrumb.AdminArea" asp-controller="@Breadcrumb.AdminController" asp-action="Index">@Breadcrumb.AdminTitle</a></li>
            <li><a asp-area="@Breadcrumb.AdminArea" asp-controller="@Breadcrumb.MembersController" asp-action="Index">@Breadcrumb.MembersTitle</a></li>
            <li>@Breadcrumb.Delete</li>
        </ol>

    </div>
</div><!-- End Breadcrumbs -->

<section class="members">
    <div class="row">
        <div class="col-lg-6" style="margin-left: auto; margin-right:auto;">

            <!-- Multi Columns Form -->
            <form enctype="multipart/form-data" method="post" asp-action="Delete">
                <input asp-for="@Model.Id" hidden />
                <div class="card">
                    <div class="card-body row">
                        <div class="col-lg-6 text-center">
                            <img src="@Model.ImageLink" />
                            <input asp-for="ImageLink" hidden />
                            <h2 class="card-title text-center">@Model.Name</h2>
                            <h4 class="card-title text-center">@Model.Role</h4>
                            <h6 class="card-title text-center">@Model.InstagramLink</h6>
                        </div>
                        <div class="col-lg-6">
                            <textarea id="readonly" asp-for="@Model.Description" class="form-control"></textarea>
                        </div>
                        <div class="mt-3 text-center">
                            <a asp-area="@Breadcrumb.AdminArea" asp-controller="@Breadcrumb.MembersController" asp-action="Index" class="back-btn">@Breadcrumb.Back</a>
                            <button type="submit" class="delete-btn">@Breadcrumb.Delete</button>
                        </div>

                    </div>
                </div>

            </form><!-- End Multi Columns Form -->

        </div>
    </div>
</section>
