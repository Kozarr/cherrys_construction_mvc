@model IEnumerable<CompanyCertificateResponce>

@{
    ViewData["Title"] = Breadcrumb.CertificatesTitle;
}

<!-- ======= Breadcrumbs ======= -->
<div class="breadcrumbs d-flex align-items-center" style="background-image: url('/assets/img/breadcrumbs-bg.jpg');">
    <div class="container position-relative d-flex flex-column align-items-center" data-aos="fade">

        <h2>@Breadcrumb.CertificatesTitle</h2>
        <ol>
            <li><a asp-area="@Breadcrumb.HomeArea" asp-controller="@Breadcrumb.HomeController" asp-action="Index">@Breadcrumb.HomeTitle</a></li>
            <li><a asp-area="@Breadcrumb.AdminArea" asp-controller="@Breadcrumb.AdminController" asp-action="Index">@Breadcrumb.AdminTitle</a></li>
            <li>@Breadcrumb.CertificatesTitle</li>
        </ol>

    </div>
</div><!-- End Breadcrumbs -->


<section id="list" class="list">
    <div class="container" data-aos="fade-up">

        <div class="section-header">
            <h2>@Breadcrumb.CertificatesTitle</h2>
        </div>

        @if (Model.Any())
        {
            <div class="text-center">
                <div class="container" data-aos="fade-up">

                    @foreach (var item in Model)
                    {
                        <div class="row gy-4 list-item featured mt-4" data-aos="fade-up" data-aos-delay="100">
                            <div class="col-lg-8 col-md-12 col-sm-12">
                                <div class="col-lg-12 align-items-center justify-content-center">
                                    <p>@Html.Raw(@item.Description)</p>
                                </div>
                            </div>
                            <div class="col-lg-4 col-md-12 col-sm-12 d-flex justify-content-around align-items-center mb-3">
                                <div class="text-center"><a class="edit-btn" asp-area="@Breadcrumb.AdminArea" asp-action="Edit" asp-route-id="@item.Id">@Breadcrumb.Edit</a></div>
                                <div class="text-center"><a class="delete-btn" asp-area="@Breadcrumb.AdminArea" asp-action="Delete" asp-route-id="@item.Id">@Breadcrumb.Delete</a></div>
                            </div>
                        </div>
                    }

                </div>
                <div class="col-12 add-btn-container text-center" data-aos="fade-up" data-aos-delay="100">
                    <a class="add-button" asp-area="@Breadcrumb.AdminArea" asp-action="Create"><i class="bi bi-plus"></i></a>
                </div>

            </div>
        }
        else
        {
            <div class="col-12 add-btn-container text-center" data-aos="fade-up" data-aos-delay="100">
                <a class="add-button" asp-area="@Breadcrumb.AdminArea" asp-action="Create"><i class="bi bi-plus"></i></a>
            </div>
        }

    </div>
</section>